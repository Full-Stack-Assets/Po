# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# LLM Providers
openai>=1.60.0
anthropic>=0.40.0
mistralai>=1.3.0
aiohttp>=3.10.0

# Trust layer — DNS lookups for the email-deliverability verifier
dnspython>=2.6.0
# Trust layer — durable persistence (optional; in-memory store used if absent)
asyncpg>=0.29.0

# Dev / Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
python-dotenv>=1.0.0
